import * as React from "react"; interface Props { children?: React.JSX.Element;
} export const
{{pascalCase name}}
= ({ children }: Props) => { return (
<div>
	<h1>{{name}}</h1>
	{children}
</div>
); };